# ROTINA — melhoria contínua do Jazz Conecta (a cada 4 horas)

Cópia versionada do prompt da rotina `trig_012VQrqFsYzW3r8hPgA2xei4`
(*"Jazz Conecta — melhoria contínua do sistema (a cada 4h)"*, cron `53 */4 * * *` UTC,
disparo ligado à sessão persistente que a criou).

O trigger é a fonte que executa; este arquivo existe para que a rotina tenha
histórico em git. **Quando um mudar, mude o outro no mesmo commit.**

Pedido do Pablo (03/09/2026): rotina de melhoria contínua, a cada 4 horas, do
sistema/site https://jazz-conecta-imoveis.netlify.app/ — construção, mudanças,
correção de bugs e acompanhamento dos logs de pesquisas recentes.

## O que foi verificado antes de escrever a rotina (03/09/2026)

| Fonte | Achado |
|---|---|
| Netlify (time `imobjazz`) | sites: jazz-conecta-oportunidades, jazz-busca-imoveis-demo, jazz-tour-virtual (+ cópia cd). **`jazz-conecta-imoveis` não está neste time** |
| Lovable (28 projetos) | nenhum com esse nome; o app do Jazz é `jazz-lead-conecta` (657a4099-…) |
| Cloudflare Workers | `jazz-lead-conecta`, `jazz-feed-vrsync`; nada com esse nome |
| Repositórios | `jazz-lead-conecta` (fonte do sistema), `maquina-yt-receita` (pasta `jazz/`), `proximo-cliente` (não relacionado) — nenhum cita o domínio |
| Egress do ambiente | netlify.app, supabase.co, workers.dev e lovable.app bloqueados (curl → 000). Só MCP alcança |
| Supabase `cscczluzpblzhvojxanp` | **todas as Edge Functions em HTTP 402** (cota estourada), inclusive `buscar-imoveis`; `busca_feedback` vazia; último erro em `app_logs` 14/08 |
| Rotinas existentes | 5 rotinas Jazz anteriores; 3 auto-desativadas por perda de acesso ao repo. Nenhuma cobre este escopo |

Conclusão: a rotina trabalha pelo código-fonte real (`jazz-lead-conecta`) e
pelos logs do Supabase, e trata o domínio Netlify como superfície pública a
conferir quando o conector permitir.

## Pendência humana

A criação via MCP não grava conectores no trigger. Se a primeira rodada
reportar "sem conector Supabase", anexar Supabase (e Netlify) à rotina na tela
de Rotinas do claude.ai — sem isso o passo 1 (logs) não roda.

---

Rodada de MELHORIA CONTÍNUA do sistema Jazz Conecta (site público https://jazz-conecta-imoveis.netlify.app/). Pedido do Pablo em 03/09/2026: a cada 4 horas, construção, mudanças, correção de bugs e acompanhamento dos logs de pesquisas recentes. Esta instrução é autônoma e completa: o contexto anterior desta sessão pode ter sido resumido, então não dependa dele.

COMECE invocando e seguindo a skill `jazz-conecta-master-ai` (fonte de verdade do produto, dos papéis e do formato de resposta). Não peça confirmação: o Pablo não está na sessão. Trabalhe até entregar, ou encerre em silêncio se não houver nada seguro a fazer.

ONDE O SISTEMA VIVE (verificado em 03/09/2026):
- Código-fonte: repositório GitHub `sampaioopablo1-lgtm/jazz-lead-conecta`, branch principal `main`. Clone em /home/user/jazz-lead-conecta (se já existir, `git fetch origin main && git checkout main && git pull`; se não, `add_repo` + `git clone --depth 1`, timeout generoso, um clone por vez). Leia `AI_HANDOFF.md` ANTES de mexer e registre sua rodada no topo do `## Log` DEPOIS (regra obrigatória do repositório). Este repositório é o ÚNICO alvo de código; nunca tocar em `maquina-yt-receita` (máquina de vídeos do YouTube — projeto separado, ordem do Pablo), exceto para manter `jazz/ROTINA-MELHORIA-CONTINUA.md` (cópia versionada deste prompt) em sincronia quando este prompt mudar.
- O SITE DO PEDIDO, https://jazz-conecta-imoveis.netlify.app/, É A LP `lp-oportunidades/` deste repositório (descoberto em 04/09 pelo bot do Netlify no PR #44: o site está ligado ao repo e publica `lp-oportunidades/public` + a função `netlify/functions/buscar.mjs` em `/api/buscar`, que chama a GeckoAPI; regra toda em `buscar-core.mjs`; testes `node teste-buscar.mjs` (121 na main desde o #52) e `node teste-sugestao.mjs` (só existe no #53), sem rede; NÃO usa Supabase). Ele mora em OUTRA conta/time Netlify, não no `imobjazz`, então os logs da função `/api/buscar` NÃO são legíveis pelo MCP até o Pablo mover o site para o time `imobjazz` ou conectar essa conta. Registro por busca (uma linha JSON, sem dado pessoal) entrou no PR #49; até ele ir para a main, a função não registra nada.
- Outras superfícies públicas do app: página de busca `src/pages/BuscaImoveisPage.tsx` + `src/routes/busca-imoveis.tsx` (busca semântica em `src/lib/busca-semantica.functions.ts`, feedback em `busca_feedback`); Edge Function `supabase/functions/buscar-imoveis` (Apify, deploy pelo workflow `deploy-busca-imoveis.yml`); LP autônoma `lp-oportunidades/` (Netlify `jazz-conecta-oportunidades`, GeckoAPI). App principal roda no Cloudflare Worker `jazz-lead-conecta` (https://jazz-lead-conecta.pablo-jazzimob.workers.dev) e no Lovable (project_id 657a4099-ae1a-4391-9fad-357cd7a9041a).
- Sites Netlify no time `imobjazz` (legíveis pelo MCP): extraordinary-florentine-6fcac4 (app, ligado ao repo), jazz-conecta-oportunidades, jazz-busca-imoveis-demo, jazz-tour-virtual. O egress deste ambiente BLOQUEIA netlify.app, supabase.co, workers.dev e lovable.app (curl/WebFetch devolvem 000/403) — só as ferramentas MCP (Supabase, Netlify, Lovable, Cloudflare, GitHub) alcançam esses serviços. Não gaste rodada tentando curl; se um dia o Netlify MCP listar o site, use `get-project`/`get-forms-for-project`/deploys dele como fonte primária.
- Banco: Supabase `cscczluzpblzhvojxanp` (o antigo `wgspmezevbjsswctefck` NÃO é mais o banco). SELECT à vontade via `mcp__Supabase__execute_sql`; INSERT/UPDATE/DDL em produção JAMAIS sem ordem explícita do Pablo (migration versionada em PR é o caminho).
- Se as ferramentas MCP do Supabase não estiverem disponíveis na rodada, registre isso no handoff como pendência humana ("rotina sem conector Supabase — anexar na tela de Rotinas do claude.ai") e faça só o que dá com GitHub + código (passos 2 a 6).

ESTADO CONHECIDO EM 03/09/2026 (confira antes de agir, pode ter mudado):
- CRÍTICO: todas as Edge Functions do projeto (buscar-imoveis, smart-feed-nativo, captacao-prospectar, geocode-enderecos, medir-fotos, visita) respondem HTTP 402 — cota do Supabase estourada (egress/storage). Os PRs #18 (compactar banco e cortar egress) e #19 (feed dos portais pelo Cloudflare) atacam isso. Enquanto o 402 durar, qualquer busca no site que dependa da Edge Function falha para o usuário final.
- PRs draft abertos: #4, #5, #13, #16, #18, #19. Deploy-preview do site `extraordinary-florentine-6fcac4` falha em TODO PR com "Build blocked: Unrecognized Git contributor" (verificação estrita de contribuidor do plano Netlify; o build nem começa) — não é do PR, já comentado em #44, não recomentar; pendência do Pablo. Deploy-preview do `jazz-conecta-imoveis` só roda quando `lp-oportunidades/` muda; "canceled" = ok.
- Rodada 1 (04/09 00:55Z) mediu: 402 em TODO o projeto (REST /config 1.038x, vista_webhooks_recebidos 744x POST perdidos, realtime, auth, storage), 1 chamada a buscar-imoveis em 24h (402), busca_feedback 0, 9 ERROR "Direct deletion from storage tables" no postgres_logs (investigar quem faz DELETE em storage.objects), advisors: 6 views SECURITY DEFINER, 2 funções SECURITY DEFINER executáveis por anon. vigia_site 200 e cron sem falha ENGANAM: o HTML responde e o pg_cron roda dentro do banco; só o net.http_* leva 402.
- Rodadas 2-4 (04/09 04:53Z-12:53Z): 402 segue total (318 em 8h, depois 400 em 4h; webhooks do Vista perdidos 87 + 89; 8 visitantes reais de tour recusados), 0 buscas de usuário no app, 0 linhas em busca_feedback/consultas_portais_log/app_logs. Os ERROR "Direct deletion from storage tables" eram comandos manuais via mgmt-api (sessões MCP), não job — nada a consertar. Backlog d (SOLIC_STATUS) e e (calcScore/precoImovel) já estavam resolvidos na main; saíram da lista. Rodada 5 (16:53Z): 404×402 em 4h, 98 webhooks perdidos, 3 visitantes de tour recusados; postgres_logs sem ERROR; dev_agent_tasks com 10 abertas paradas desde 04/08 (não é novidade). Rodada 6 (20:53Z): 454×402, 138 webhooks perdidos (subindo), 1 visitante de tour recusado; main = bf64c68 (PR #48: metade da LP para pessoa física + Gemini no lugar do gateway Lovable) e a main foi mergeada nos PRs da rotina sem conflito. PR #52 (outra sessão: prazo por portal, cache de payload, deduplicação) ENTROU na main em 05/09 09:43Z (main = 70a57a6); a rotina mergeou essa main nos 7 PRs dela às 10:40Z (conflitos só em `teste-buscar.mjs` do #49, resolvido juntando os blocos, e no README do #53). Com o cache do #52, `buscar-core.mjs` voltou a ser terreno livre para incrementos; o registro por busca do #49 envolve `buscarOpcoes` por fora, então busca servida pelo cache também gera linha de log. Rodadas 7-8 (05/09 00:53Z e 04:53Z, fechadas juntas): 737×402 em 8h, 130 webhooks perdidos, 5 visitantes de tour recusados; XML: Storage, REST e Lovable/Worker antigo mortos; Worker `jazz-feed-vrsync` vivo e com cron confirmado (55/26); PR #54 a pedido do Pablo faz a URL do Netlify servir o XML do Worker. A URL cadastrada nos portais é desconhecida daqui: pendência do Pablo. Rodada 9 (08:53Z): 313×402 em 4h, 0 webhooks do Vista perdidos (o Vista não enviou nenhum nessa janela, não é melhora), 3 visitantes de tour recusados; XML: assinatura 59 / geração 26 (cron vivo, acervo sem mudança desde 02:47Z), feed_properties ativos 3.374; PR #55 aplica à rota oficial do app (`/api/public/feed/vrsync.xml`) o mesmo Worker-primeiro do #54 — vale a partir do próximo deploy do app. Rodada 10 (12:53Z): ~319×402 em 4h, 8 webhooks do Vista perdidos, 2 visitantes de tour recusados, 0 buscas no app; XML: 63 / 26 (cron vivo, acervo parado desde 02:47Z); PR #56 deixa a LP mobile first nos alvos de toque (Ligar e Ver no portal tinham 24px; agora tudo ≥ 44px, Enter busca em todos os campos) — auditoria em Chromium a 390/360/1280 com `/api/buscar` dublado; a página exige `codigo` preenchido antes do fetch, então o smoke precisa setar `#codigo` além de `#cidade`. Rodada 11 (16:53Z): ~404×402 em 4h, 100 webhooks do Vista perdidos (o item que mais cresce de dia), 0 visitantes de tour; XML: 67 / 26 (cron vivo). Diagnóstico: quem faz os POSTs em 402 é o próprio app (`src/routes/api/public/webhooks/vista.ts`, grava pela REST); `vista_webhooks_recebidos` sem linha desde 24/08 22:59Z; payload real do Vista tem só a chave `code`. PR #57 versiona o Worker `jazz-vista-webhook` (mesmo contrato, grava por `pg`, 28 testes sem rede, NÃO publicado — deploy e segredos são do Pablo) e faz o app repassar ao Worker (`VISTA_WEBHOOK_WORKER_URL`) quando a gravação falha. Risco anotado: `vista.ts` carrega um segredo fixo de reserva no código (`VISTA_FIXED_SECRET`); nunca copiar o valor, recomendar rotação. Workers na conta em 05/09: jazz-feed-vrsync, jazz-lead-conecta (03/08), throbbing-field-73e2.
- `app_logs`: último erro 14/08 (`removeChild` em /admin?tab=equipe e ?tab=historico, e React #418 de hidratação) — causa provável fechada no PR #51 (lang="pt-BR"); conferir app_logs depois do deploy. `busca_feedback`: 0 linhas. `vigia_site_runs` monitora https://jazz-lead-conecta.lovable.app/solicitar de hora em hora. `dev_agent_monitor` (pg_cron horário) cria `dev_agent_tasks` a partir de `app_logs`.

CADA RODADA, NESTA ORDEM:
1) LOGS DE PESQUISAS RECENTES (obrigatório, é a parte "acompanhamento" do pedido): (a) `mcp__Supabase__query_logs` últimas 4-24h: `source='function_edge_logs'` filtrando `buscar-imoveis` (volume, status por código, latência, erros); `edge_logs` com status >= 400 nas rotas de busca/RPC; `postgres_logs` com erro. (b) SQL: `busca_feedback` novas linhas (consulta, util, nenhum_bom, cobertura_pct); `app_logs` desde a última rodada agrupado por assinatura; `vigia_site_runs` com ok=false; `cron.job_run_details` com status='failed' nas últimas 24h; `dev_agent_tasks` pending/failed/aguardando_aprovacao; `consultas_portais_log` recentes com total_retornado=0 ou erro. (c) `mcp__Supabase__get_advisors` security e performance quando houver DDL nova ou 1x/dia. (d) Para o site em si (a LP): enquanto a função não logar e o site estiver fora do MCP, não há log de busca nenhum — diga isso em números (0) e trate o item a0 do backlog como prioridade. (e) SAÚDE DO XML DOS PORTAIS: o único caminho vivo é o Worker `jazz-feed-vrsync` do Cloudflare (conecta direto no Postgres, serve do KV `jazz-feed`, código em `workers/jazz-feed-vrsync/` pelo PR #54). Medir a cada rodada em `pg_stat_statements` as `calls` da consulta de assinatura (`n_ativos … n_ia`) e da consulta completa do feed (`fp.id, fp.codigo_original … feed_ai_content`): assinatura sobe ~1 por hora e a geração só quando o acervo muda (baseline 05/09 04:54Z: 55 / 26; 08:53Z: 59 / 26; 12:53Z: 63 / 26; 16:53Z: 67 / 26). Assinatura parada por mais de 2h = cron do Worker morto = KV servindo cópia velha → incidente, avisar o Pablo (regenera com POST /atualizar e o token do painel). Registrar também `feed_properties` ativos e `max(updated_at)`. Resuma em números: buscas, taxa de erro, consultas sem resultado, termos/filtros mais pedidos — isso alimenta o backlog (o que o usuário procura e não acha é a melhoria mais valiosa).
2) PRs DESTA ROTINA (os que ela mesma abriu — até 04/09: #44, mensagem por status na busca pública do app; #49, uma linha de log por busca na LP; #50, uma linha de log por busca na Edge Function buscar-imoveis do app; #51, html lang="pt-BR" contra o removeChild do /admin; #53, estado vazio da LP com botões que tiram um filtro; #54, XML dos portais pelo Worker do Cloudflare + código do Worker versionado; #55, a URL oficial do XML no app também tenta o Worker primeiro; #56, LP mobile first: alvos de toque de 44px e Enter que busca; #57, webhook do Vista gravado direto no Postgres pelo Worker `jazz-vista-webhook` + app repassando ao Worker quando o Supabase falha): mergeabilidade, CI no head atual, threads de review. Conflito → merge da base para dentro da branch (nunca rebase, nunca force-push). Responder review humano. Não mexer nos PRs de outras rotinas/pessoas além de ler.
3) UM INCREMENTO por rodada — construção, mudança ou correção de bug — pequeno, entregável e validado. Prioridade: (i) bug que aparece nos logs do passo 1 e afeta usuário; (ii) melhoria que os logs de busca pedem (consulta sem resultado, filtro ausente, mensagem de erro sem ação, tempo de resposta); (iii) item do backlog abaixo. Bug de código → teste de regressão escrito ANTES do fix (vitest). Nada seguro que caiba na rodada → só refinar o backlog no handoff e encerrar em silêncio.
4) VALIDAÇÃO antes de qualquer push (regras aprendidas, obrigatórias): `npm ci` falha na main (package-lock fora de sync, defeito pré-existente) — use `npm install --no-package-lock --no-audit --no-fund`; `npx tsc --noEmit` (baseline da main em 04/09: 1 erro conhecido em src/router.tsx; ZERO erro novo é o critério — rode também na main via git stash para comparar); `npm run build`; `node scripts/check-server-import-leaks.mjs` se existir e grep no dist/client garantindo que nenhum módulo `.server.ts` vaza para o navegador; `npx vitest run` se houver testes (em 04/09 o repo não tinha vitest nem arquivo *.test.* — dívida registrada; não invente suíte inteira numa rodada). DEPOIS de todo build, SEMPRE: `git checkout -- src/routeTree.gen.ts 'src/routes/api/public/feed/vrsync[.]xml.ts'` (o arquivo tem colchetes no nome) e `rm -rf dist .output .tanstack .nitro .netlify` (desde o #45 o build do app sai em `.netlify/`, não em `dist/`; o grep de vazamento vale para o diretório de saída que existir) — o codegen reescreve a rota do feed e isso NUNCA pode ser commitado (quebra o XML dos portais). Para `lp-oportunidades`: `node teste-buscar.mjs && node teste-sugestao.mjs`; mudança na página (`public/index.html`) pede também um smoke em Chromium headless (playwright global em /opt/node22, servidor estático em `public/`, `/api/buscar` dublado por `page.route`).
5) ENTREGA: branch `claude/melhoria-continua-<assunto>` (nunca commitar na main), commits claros em pt-BR, `git push -u origin`, PR DRAFT (não duplicar PR já aberto para o mesmo assunto; assunto já coberto por PR existente → comentar lá em vez de abrir outro). Nunca incluir identificador de modelo em commit, PR, código ou doc.
6) REGISTRO: entrada no topo do `## Log` do `AI_HANDOFF.md` (data/hora, objetivo, arquivos/rotas/tabelas, PR, validações, pendências, riscos) + `docs/ai-change-log.md` quando houver mudança de código. Números dos logs de busca da rodada entram no handoff (compare com a rodada anterior).
7) COMUNICAÇÃO: mensagem ao Pablo (resposta final desta rodada) só quando entregar incremento relevante, detectar incidente (site fora do ar, 402/5xx em massa, cron falhando em série) ou precisar de decisão. Rodada sem novidade = silêncio (sem comentário em PR, sem mensagem).

BACKLOG INICIAL (reordene conforme os logs ensinarem; estado de 04/09):
a0. [EM PR #49 — não refazer; cuidar do PR] Registrar cada busca da LP: uma linha JSON por chamada em `buscar-core.mjs` (parâmetros, portais consultados, total por portal, motivo, duração; IP truncado; NUNCA nome/telefone) — log de função no painel do Netlify sem depender do Supabase. `node teste-buscar.mjs` = 129 verde (121 da main + 8 do registro). Lado do app: PR #50 faz o mesmo na Edge Function `buscar-imoveis` (evento busca_app); depois do merge, o passo 1(a) passa a ler essas linhas em `function_edge_logs`.
a1. [EM PR #57 — não refazer; cuidar do PR] Webhook do Vista sem passar pela API do Supabase (Worker `jazz-vista-webhook` + repasse no app). Fechar quando `select count(*) from vista_webhooks_recebidos where recebido_em > now() - interval '4 hours'` voltar a acompanhar os POSTs; até o deploy, o número de POSTs 402 em `/rest/v1/vista_webhooks_recebidos` continua sendo a medida da perda.
a. Resiliência da busca pública ao 402 do Supabase: fallback e mensagem clara ao usuário quando `buscar-imoveis` falhar (hoje a Edge Function devolve 502/402 e o site fica mudo); considerar servir a busca pelo Worker/Cloudflare como o feed no PR #19.
b. Registrar toda pesquisa da busca pública (termo, filtros, cidade, resultados_count, latência, origem) em tabela própria aditiva (migration em PR) — sem isso "logs de pesquisas recentes" depende só dos logs efêmeros do Supabase (retenção curta).
c. [EM PR #51 — não refazer] Bug `removeChild`/React #418 em /admin (tabs equipe e histórico): causa provável era `<html lang="en">` em página em português (tradução automática do Chrome mexendo no DOM). Fechar o item quando, depois do deploy, `app_logs` ficar sem novo removeChild em /admin; se voltar, a próxima suspeita é extensão do navegador do mesmo visitante (assinatura 850a67509efe8c22).
d. UX da busca: estado vazio com botões de ampliar filtro FEITO na LP (PR #53 — não refazer); alvos de toque de 44px e Enter que busca FEITO na LP (PR #56 — não refazer). Resta: 3 cliques ou menos até o primeiro resultado; teste de regressão para `motivo` fora do mapa AVISOS (a página diz "Busca indisponível agora." mesmo com resultado); no app, `src/components/BuscaImoveis.tsx` ainda tem o texto genérico "Tente ampliar a busca" — só mexer depois que o #44 entrar (mesmo arquivo).

APROVAÇÃO PERMANENTE DO PABLO cobre: código em branches claude/*, PRs draft, atualizar PRs desta rotina, SELECT e leitura de logs no banco, leitura de Netlify/Lovable/Cloudflare. NÃO cobre: merge na main, escrita/DDL direta no banco de produção, alterar o link público do XML dos portais, reformatação em massa, mexer em package-lock.json/bun.lock, deploy de Edge Function ou Worker, criar/alterar/apagar rotinas (NUNCA criar novos triggers), gastar créditos Lovable/Apify/Gecko.

RESPOSTA FINAL (quando houver o que dizer): formato obrigatório da skill, começando por "Logs de busca (últimas Xh)" com os números, depois o incremento entregue (PR + link), pendências humanas e o backlog reordenado.
