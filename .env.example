# ============================================================
# Copie para .env e preencha. O .env NUNCA vai para o git.
# Qualquer provider sem chave cai automaticamente no stub
# offline, entao a pipeline roda mesmo sem credencial nenhuma.
# ============================================================

# ---- Roteiro / ideacao (LLM) ----
# Providers: auto | anthropic | gemini | openai | stub
# auto = usa a primeira chave presente: Anthropic > Gemini > OpenAI.
# Plano B GRATUITO: GEMINI_API_KEY do AI Studio (aistudio.google.com),
# free tier sem cartao — cobre o ritmo diario do canal.
MAQ_LLM_PROVIDER=auto
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
MAQ_LLM_MODEL=claude-sonnet-4-5
OPENAI_API_KEY=

# ---- Narracao (TTS) ----
# Providers: modal | lote | fish | elevenlabs | openai | stub
# modal = GRATUITO e AUTOMATICO (padrao de config/default.yaml): Chatterbox
#         indonesio como endpoint serverless. Setup unico com `modal deploy
#         infra/modal_tts.py`, depois preencha MAQ_TTS_URL/MAQ_TTS_TOKEN abaixo.
#         Ver docs/09-voz-gratuita.md
# lote =  GRATUITO mas MANUAL: narracao no Colab (Chatterbox indonesio) com a
#         voz clonada de assets/voice/referencia.wav.
# fish = pago (~US$0,12/video). ATENCAO: a chave anterior vazou — revogue.
MAQ_TTS_PROVIDER=lote
MAQ_TTS_URL=
MAQ_TTS_TOKEN=
FISH_AUDIO_API_KEY=
# Para o fish, MAQ_TTS_VOICE_ID e o id do modelo de voz:
# o trecho final da URL fish.audio/m/<id>
MAQ_TTS_VOICE_ID=0f8b54a7ec2f4d328a146db341ab63ad
ELEVENLABS_API_KEY=
MAQ_TTS_MODEL=eleven_multilingual_v2

# ---- Imagens / b-roll ----
# Providers: openai | stub
MAQ_IMAGE_PROVIDER=openai
MAQ_IMAGE_MODEL=gpt-image-1

# ---- YouTube Data API v3 ----
# 1) Google Cloud Console > APIs > ative "YouTube Data API v3"
# 2) Credenciais > OAuth client ID > tipo "Desktop app" > baixe o JSON
# 3) Aponte MAQ_YT_CLIENT_SECRET para esse arquivo e rode: maquina auth-youtube
MAQ_YT_CLIENT_SECRET=secrets/client_secret.json
MAQ_YT_TOKEN=secrets/youtube_token.json

# ---- Supabase (estado que sobrevive ao runner efemero) ----
# Sem estes dois o estado fica so no SQLite local e as views painel_pilares /
# progresso_ypp ficam vazias. Dashboard > Project Settings > API.
# Use a service_role (as tabelas tem RLS liberando so ela) e cadastre os dois
# como secrets do repositorio tambem.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# ---- Diretorios ----
MAQ_DATA_DIR=data
MAQ_OUT_DIR=out
